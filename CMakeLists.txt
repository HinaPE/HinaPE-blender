cmake_minimum_required(VERSION 3.16)
project(HinaPE-blender)
if(APPLE)
    enable_language(OBJC)
endif()
add_subdirectory(HinaPE)
add_subdirectory(pybind11)
pybind11_add_module(HinaPE-py src/main.cpp)
# EXAMPLE_VERSION_INFO is defined by setup.py and passed into the C++ code
target_compile_definitions(HinaPE-py PRIVATE VERSION_INFO=${EXAMPLE_VERSION_INFO})
